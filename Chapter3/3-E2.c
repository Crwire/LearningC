#include <stdio.h>
#include <math.h>
#include <string.h>

/*
Basic if-else testing, using indents and blank else
*/
int main()
{
	
	//Variables
	int x, y;
	x = 6;
	y = 0;
	//Process
	if (x > 5)
		if (y > 10)
			printf("Hello! \n");
		else;
	else printf("Goodbye! \n");
}